# CodeQL configuration for GrowChat
# Excludes files with known false positives from analysis

name: 'GrowChat CodeQL Config'

queries:
  - uses: security-and-quality

paths-ignore:
  - 'public/js/shared/markdown-special-block-runtime.js'
