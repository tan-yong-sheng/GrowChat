# Pages

This directory contains page-centric documentation. It acts as the root node for a specific route (e.g., `/admin/settings`), linking to the components it uses, the states it can enter, and the interactions it supports.