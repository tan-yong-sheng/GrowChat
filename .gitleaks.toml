[extend]
useDefault = true

[[allowlists]]
description = "Allow test fixtures, skill docs, and public keys — contain mock secrets only"
paths = [
  'public/.*',
  'tests/e2e/fixtures/.*',
  '\.claude/skills/.*',
  'src/routers/auth\.test\.js'
]

[secrets]
whitelist = ["scripts/secret-scan.cjs"]
