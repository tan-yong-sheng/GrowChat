# Feature Spec: Landing Page with Open-Source Positioning and Deploy CTA

> This document was auto-generated by the Git Worktree Design Skill, intended as a development guide for AI Agents.

## Branch Info

| Item            | Value                                          |
| --------------- | ---------------------------------------------- |
| Branch name     | `feature/landing-page`                         |
| Based on branch | `main` (ea8d80f3)                              |
| Worktree path   | `/mnt/data/Coding/GrowChat/.worktrees/landing` |
| Created at      | 2026-05-19                                     |
| GitHub Issue    | #33                                            |

## Goal

Create a public landing page for GrowChat that communicates the project's value proposition: zero-ops Cloudflare deployment, multi-user RBAC chat, and self-serve LLM workspace. This is the signup funnel — no landing page means no conversion.

## Implementation Scope

- [x] Create `public/landing.html` — static marketing page with:
  - Hero section: tagline ("30-second deploy. Zero Docker. Chat with any LLM."), deploy CTA button, GitHub stars badge
  - Features section: 3 key pillars (RBAC, Multi-provider LLM, Cloudflare deploy)
  - Comparison section: vs Open WebUI (needs Docker), vs LibreChat (no RBAC), vs HiveChat (no 3-scope permissions)
  - Pricing section: "Free & Open Source" with MIT license badge, Cloudflare free tier = free hosting
  - Footer: GitHub repo link, MIT license, deploy CTA
- [x] Update `src/index.js` — route handling for unauthenticated `/`:
  - Unauthenticated users hitting `/` serve `landing.html`
  - Authenticated users hitting `/` redirect to the SPA (`/index.html`)
- [x] Create `public/js/features/landing.js` (minimal JS) — GitHub stars fetch, smooth scroll, mobile nav toggle
- [x] Responsive design matching DESIGN.md aesthetics (low-density, pill geometry, Action Blue)
- [x] No authentication required to view the landing page
- [x] Update `docs/` wiki with landing page entry in UI/UX mapping

## Acceptance Criteria

- Public route `/` for unauthenticated users serves the landing page
- Landing page includes: hero with deploy CTA, features section, pricing (free/open-source), GitHub link
- Responsive design — mobile, tablet, desktop all render correctly
- DESIGN.md compliance: low-density layout, pill geometry, Action Blue (#3B82F6) accent color
- Authenticated users skip landing page and go directly to the chat SPA
- Lighthouse Performance score > 90 (mostly static content, should be easy)
- No authentication required to view

## Technical Constraints

- Static HTML + Tailwind CSS — no React, no build step for the landing page itself
- Must run `pnpm run build:css` if adding new Tailwind classes
- Minimal JS — GitHub stars API fetch (`https://api.github.com/repos/tan-yong-sheng/GrowChat`) and smooth scroll only
- Must work on Cloudflare Workers as a static asset served via the ASSETS binding
- No new npm dependencies
- Route logic in `src/index.js` must check for JWT cookie/header before deciding landing vs SPA

## Cross-branch Notes

- **Merge order: ANYTIME** — Fully independent. New files mostly, minimal `src/index.js` route addition.
- Can be developed in parallel with all other branches.
- If `feature/one-deploy` (#34) lands first, the deploy CTA button can link to the setup wizard instructions.
- If `feature/structured-logging` (#37) lands first, the new route handler in `src/index.js` should use the logger instead of console.log.
