# Feature Spec: Admin Usage Overview Dashboard

> This document was auto-generated by the Git Worktree Design Skill, intended as a development guide for AI Agents.

## Branch Info

| Item            | Value                                            |
| --------------- | ------------------------------------------------ |
| Branch name     | `feature/admin-dashboard`                        |
| Based on branch | `main` (ea8d80f3)                                |
| Worktree path   | `/mnt/data/Coding/GrowChat/.worktrees/dashboard` |
| Created at      | 2026-05-19                                       |
| GitHub Issue    | #35                                              |

## Goal

Build an admin dashboard at `/admin/system/usage` that shows workspace usage metrics: total users, active users (7d/30d), messages sent per day/week, and LLM API calls (sparks). Data sourced from existing D1 tables — no new tables needed for MVP.

## Implementation Scope

- [x] Create new API endpoint `GET /api/admin/usage` in `src/routers/admin.js`:
  - Total users count
  - Active users (last 7 days, last 30 days) — from `users.last_active_at`
  - Messages sent (per day for last 7 days, per week for last 4 weeks) — from `messages` table
  - LLM sparks count — from `messages` where `role = 'assistant'` (proxy for API calls)
- [x] Add RBAC protection — admin-only access via `ensureAdminAclAccess`
- [x] Create admin dashboard UI component `public/js/features/admin/system/usage.js`:
  - Card layout with key metrics (total users, active 7d, active 30d, messages, sparks)
  - Trend indicators (↑↓→) comparing current period vs previous period
  - Simple data tables for daily/weekly breakdowns
- [x] Update `public/js/features/admin/admin-route-state.js` — add `/admin/system/usage` route
- [x] Update `public/js/bootstrap/app.js` — add admin overview route rendering
- [x] Update admin navigation — add "Usage" sub-tab under System in admin sidebar
- [x] Charts/graphs optional for MVP — plain numbers with trend indicators are sufficient
- [x] Add unit tests for the `/api/admin/usage` endpoint
- [x] Update `docs/` wiki with admin dashboard documentation

## Acceptance Criteria

- New admin route `/admin/system/usage` shows a dashboard
- Dashboard displays: total users, active users (7d/30d), messages sent (per day/week), LLM API calls (sparks)
- Data sourced from existing D1 tables (users, messages) — no new tables for MVP
- RBAC enforced — admin-only access
- Trend indicators show direction vs previous period
- Responsive layout matching DESIGN.md aesthetics

## Technical Constraints

- Use existing D1 queries — no new tables or migrations for MVP
- Admin-only access enforced via `src/utils/authorize.js` (`ensureAdminAclAccess`)
- UI must follow DESIGN.md: low-density, pill geometry, Action Blue palette
- No new npm dependencies — use vanilla JS + Tailwind CSS
- Consider query performance on D1 — avoid full table scans, use `COUNT` with `WHERE` date filters
- If structured logging (#37) has landed, use `logger.info()` instead of `console.log()`

## Cross-branch Notes

- **Merge order: THIRD** (after `feature/structured-logging` #37) — This branch touches `src/routers/admin.js` which #37 also modifies. Rebasing onto post-#37 main avoids console.log→logger conflicts.
- No overlap with `feature/google-oauth` (#36), `feature/one-deploy` (#34), or `feature/landing-page` (#33).
- Future enhancement: add charts (Chart.js or similar) after MVP is validated.
