# Feature Spec: Lint Cleanup — Fix All Errors & Warnings

> This document was auto-generated by the Git Worktree Design Skill, intended as a development guide for AI Agents.

## Branch Info

| Item            | Value                                               |
| --------------- | --------------------------------------------------- |
| Branch name     | `feature/lint-cleanup`                              |
| Based on branch | `dev`                                               |
| Worktree path   | `/mnt/data/Coding/GrowChat/.worktrees/lint-cleanup` |
| Created at      | 2026-05-20                                          |

## Goal

Achieve **0 errors, 0 warnings** on `pnpm run lint`, and add `pnpm run lint` to the `prepush` hook so it's enforced on every push.

## Implementation Scope

### Phase 1 — Fix all 7 errors (must pass `--max-warnings=0` for lint-staged)

- [x] Fix 3× `no-useless-assignment` in `src/routers/admin.js` (lines 934, 1059, 1236)
- [x] Fix 3× `no-extra-boolean-cast` in `public/js/features/admin/settings/registration.js` (lines 39–41)
- [x] Fix 1× `no-undef` (`setModalSaveButtonState`) in `public/js/features/admin/settings/integrations.js` (line 233)
- [x] Fix 1× `no-duplicate-imports` in `src/routers/admin.js` (line 61)

### Phase 2 — Add `pnpm run lint` to `prepush`

- [x] Add `pnpm run lint` to the `prepush` script in `package.json` (before or after `typecheck`)

### Phase 3 — Eliminate all 572 warnings

- [x] **152× `boundaries/no-unknown-files`** — Add `src/` element patterns to `eslint.config.cjs` `boundaries/elements` (e.g., `s-router`, `s-service`, `s-shared`, `s-utils`, `s-feature`, `s-admin`, `s-llm`, `s-chat`, `s-mcp`, `s-middleware`, `s-repository`, `s-bootstrap`, `s-config`, `s-durable`, `s-errors`, `s-validation`)
- [x] **40× `no-unused-vars`** — Remove dead variables: `showToastProgress`, `persistDefaultModelSelection`, `isDefaultModel`, `providerValue`, `usingFilter` (×2), `countLabel`, `clearButtonId`, `access_allowed`, `e` (catch block), `vi`
- [x] **35× `max-nested-callbacks`** — Flatten nested callbacks in test files to `async/await`
- [x] **31× `max-depth`** — Extract deeply nested blocks into helper functions
- [x] **195× `complexity`** — Break down high-complexity functions (worst: `_handleParsed` 119, `adminRouter` 415, `modelsRouter` 214, `usersRouter` 337)
- [x] **116× `max-lines-per-function`** — Extract sub-functions from oversized functions (worst: `wireChat` 1037 lines, `adminRouter` 1628 lines)
- [x] **3× `no-duplicate-imports`** — Merge duplicate import statements

## Acceptance Criteria

- `pnpm run lint` exits with code 0 (0 errors, 0 warnings)
- `pnpm run prepush` includes `pnpm run lint` in its pipeline
- `pnpm run test` still passes (no regressions)
- `lint-staged` with `--max-warnings=0` works cleanly for new commits

## Technical Constraints

- No new npm dependencies
- Must not break existing functionality — all tests must pass
- Test files with `max-lines-per-function` warnings should use `async/await` to reduce nesting, not just split artificially
- Complexity reductions must preserve business logic — extract helpers, don't just wrap in early returns
- The `boundaries/elements` additions should follow the same pattern as existing frontend entries

## Cross-branch Notes

- No dependencies on other worktree branches
- Merge this before any feature branches that touch `src/routers/admin.js` or `public/js/features/admin/` to avoid conflicts
- The `boundaries/no-unknown-files` fix (adding `src/` element types) will also need corresponding `boundaries/dependencies` rules to enforce src-layering — consider adding those as a follow-up issue
