# Feature Spec: Google OAuth/SSO Login Integration

> This document was auto-generated by the Git Worktree Design Skill, intended as a development guide for AI Agents.

## Branch Info

| Item            | Value                                        |
| --------------- | -------------------------------------------- |
| Branch name     | `feature/google-oauth`                       |
| Based on branch | `main` (ea8d80f3)                            |
| Worktree path   | `/mnt/data/Coding/GrowChat/.worktrees/oauth` |
| Created at      | 2026-05-19                                   |
| GitHub Issue    | #36                                          |

## Goal

Add Google OAuth 2.0 as a login method alongside the existing email+password auth. Users should be able to sign up and log in with their Google account, with auto-provisioning and email-matching for existing users.

## Implementation Scope

- [x] Add new env vars to `wrangler.jsonc`: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` as secret bindings
- [x] Add Google OAuth routes in `src/routers/auth.js`:
  - `GET /api/auth/google` — redirect to Google OAuth consent screen
  - `GET /api/auth/google/callback` — exchange auth code for user info, create/link account, issue JWT + refresh token
- [x] Implement `state` parameter (CSRF protection) — store in KV with short TTL
- [x] Auto-provisioning: new Google users get a local account created with `member` role and `active` status
- [x] Email matching: if Google email matches an existing local user, link the accounts (add `google_id` to user record)
- [x] Add `google_id` column to users table — new migration `migrations/004_google_oauth.sql`
- [x] Update `src/services/auth.js` (or equivalent) to handle Google-authenticated sessions with same JWT + refresh token flow
- [x] Update auth page UI (`public/js/features/auth/` or `public/auth.html`) — add "Sign in with Google" button alongside email/password form
- [x] Fallback: local auth still works when `GOOGLE_CLIENT_ID` is not configured — detect env var presence, conditionally show Google button
- [x] Add RBAC integration — Google-authenticated users go through same `ensureAuth` middleware
- [x] Add unit tests for OAuth flow (callback handling, auto-provisioning, email matching)
- [x] Update `docs/` wiki with OAuth flow documentation

## Acceptance Criteria

- User clicks "Sign in with Google" → redirected to Google consent screen → callback creates/issues JWT
- New Google users: account auto-created with `member` role, `active` status
- Existing users with matching email: accounts linked via `google_id`
- Local auth still works when Google OAuth is not configured (no GOOGLE_CLIENT_ID)
- Google-authenticated users get same JWT (HS256, 15-min TTL) + refresh token (7-day, KV-stored) flow
- CSRF protection via `state` parameter stored in KV
- "Sign in with Google" button only appears when GOOGLE_CLIENT_ID is configured

## Technical Constraints

- Use Cloudflare Workers `fetch()` for the token exchange with Google — no external OAuth libraries
- Google OAuth 2.0 endpoints: `https://accounts.google.com/o/oauth2/v2/auth` (authorize), `https://oauth2.googleapis.com/token` (token exchange), `https://www.googleapis.com/oauth2/v3/userinfo` (user info)
- Must maintain compatibility with existing auth flow — same JWT format, same refresh token mechanism
- `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` are wrangler secrets — never logged or exposed in responses
- Migration `004_google_oauth.sql` must be backwards-compatible (additive column only)
- Use existing `src/services/audit-logging.js` for security event logging (failed attempts, new account creation)

## Cross-branch Notes

- **Merge order: SECOND** (after `feature/structured-logging` #37) — This branch touches `src/routers/auth.js` which #37 also modifies. Rebasing onto post-#37 main avoids console.log→logger conflicts.
- If `feature/structured-logging` has already landed, use `logger.info()` instead of `console.log()` in new code.
- No overlap with `feature/admin-dashboard` (#35), `feature/one-deploy` (#34), or `feature/landing-page` (#33).
